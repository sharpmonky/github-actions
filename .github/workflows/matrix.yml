name: Matrix
on: pull_request

jobs:
  node-version:
    runs-on: ubuntu-latest
    steps:
      - name: log node version
        run: node -v
      - name: setup-node
        uses: actions/setup-node@v1
        with:
          node-version: 6
      - name: log node version
        run: node -v
